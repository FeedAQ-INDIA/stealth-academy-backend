# Organization Invitation System - Environment Variables
# Add these to your .env file

# Email Configuration (Required for sending invitation emails)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@yourdomain.com
SMTP_PASS=your-email-password

# Frontend URL (for invitation accept links)
FRONTEND_URL=http://localhost:3000

# Hostinger Email Setup Instructions:
# 1. Go to your Hostinger hPanel
# 2. Navigate to Email > Email Accounts
# 3. Create or use an existing email account
# 4. Use your full email address as SMTP_USER
# 5. Use your email account password as SMTP_PASS
# 6. Make sure your domain's MX records are properly configured

# Alternative Hostinger SMTP Settings (if above doesn't work):
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=465
# SMTP_SECURE=true

# Example for other email providers:
# Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Outlook/Hotmail:
# SMTP_HOST=smtp.outlook.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Yahoo:
# SMTP_HOST=smtp.yahoo.com
# SMTP_PORT=587
# SMTP_SECURE=false

# iCloud:
# SMTP_HOST=smtp.mail.me.com
# SMTP_PORT=587
# SMTP_SECURE=false

# Note: If SMTP credentials are not provided, the system will:
# - Still create invitation records in the database
# - Log email details to console (for testing)
# - Mark emails as not sent in the metadata
# - Allow manual email retry later